# 废品行情通 - 项目说明

## 项目概述
为废品回收站老板打造的废品回收价格查询 PWA 应用。核心策略：**手机端内置爬虫 + 本地存储 + 离线可用**，零服务器成本。

## 技术方案
- **PWA（渐进式 Web 应用）**：可在任何手机浏览器打开，支持安装到桌面
- **IndexedDB**：替代 SQLite，完全本地化数据存储
- **Service Worker**：离线缓存，断网可用
- **Chart.js**：价格趋势折线图
- **CORS 代理**：浏览器端爬虫适配（allorigins/corsproxy/codetabs 三代理轮换）
- 零后端、零服务器、零域名

## 文件结构
```
废品每日价格/
├── index.html              # 主页面（4 Tab 布局）
├── manifest.json           # PWA 清单
├── sw.js                   # Service Worker（离线缓存）
├── css/
│   └── style.css           # 全部样式（大字体、红涨绿跌、暗色模式）
├── js/
│   ├── db.js               # IndexedDB 数据库封装
│   ├── seed-data.js        # 30+品类 + 3个月种子价格数据
│   ├── crawler.js          # 爬虫引擎（CORS代理/随机UA/智能节电/失败重试）
│   ├── charts.js           # 趋势图渲染（Chart.js）
│   ├── export.js           # 数据导出/导入（JSON/Excel）
│   └── app.js              # 主应用逻辑（4 Tab + 弹窗 + 交互）
└── data/
    └── crawler-rules.json  # 爬虫规则配置（JSON，可热更新）
```

## MVP 功能清单
- ✅ 30+ 废品品类价格查询（废纸/塑料/金属/玻璃/家电/橡胶）
- ✅ 手机端爬虫引擎（2小时定时 + WiFi/电量智能策略 + 三代理轮换）
- ✅ 爬虫规则 JSON 配置化
- ✅ 价格趋势图（7/30/90天 + 双品类对比 + 用户成交价标注）
- ✅ 手动录入价格（含行情对比提示）
- ✅ 大字体（品类20px、价格28px加粗）+ 红涨绿跌灰平
- ✅ 数据导出 JSON/Excel + 微信分享
- ✅ 完全离线可用（PWA + Service Worker）
- ✅ 收藏置顶 + 搜索筛选
- ✅ 暗色模式（跟随系统）
- ✅ 记账功能（收/卖记录 + 盈亏统计）

## 数据来源优先级
1. 🥇 爬虫抓取的行情价（自动更新，标记来源网站）
2. 🥈 用户自己录入的实际成交价（最高可信度）
3. 🥉 App 自带种子数据（出厂内置3个月参考价）

## 运行方式
```bash
# 本地预览
python -m http.server 8080
# 浏览器访问 http://localhost:8080
```

## 后续可扩展（第二阶段）
- 价格预警（本地通知）
- 二维码点对点分享
- 语音输入
- 爬虫规则在线热更新
- 转为 Flutter 原生 App（解决浏览器 CORS 限制）
